- Se ha modificado `src/components/cart/CartItem.jsx` para cambiar la etiqueta "SKU" a "Clave" y añadir un mensaje informativo sobre la cantidad de empaques que se están adquiriendo, calculando los empaques a partir de la cantidad total y la cantidad por empaque.
- Se ha modificado `src/components/cart/CartItem.jsx` para mover el mensaje de información del empaque debajo de la "Clave" del producto, dentro del div `productDetails`, para mejorar la armonía de estilos y el layout.
- Se ha modificado `src/components/cart/CartItem.module.css` para añadir una nueva regla CSS para `.packageInfo` para reducir el tamaño del texto, ajustar los márgenes superior e inferior, y darle un color más discreto.
- Se ha modificado `src/pages/OringsPage.jsx` para pasar la prop `selectedProfile` al componente `OringsSearchResults`.
- Se ha modificado `src/components/features/product/OringsSearchResults.jsx` para aceptar la prop `selectedProfile` y pasarla al componente `FichaTecnica`.
- Se ha modificado `src/components/features/product/FichaTecnica.jsx` para aceptar la prop `selectedProfile`, importar `respaldo_schema.jpg`, y usar lógica condicional para mostrar la imagen correcta (`respaldo_schema.jpg` si `selectedProfile` es 'Respaldos', de lo contrario `oring_schema.jpg`).
- Se ha modificado `src/components/features/product/FichaTecnica.jsx` para extraer dinámicamente la dureza de la prop `selectedMaterial` y mostrarla en la tabla de propiedades técnicas, en lugar de usar la dureza hardcodeada del objeto `materialProperties`.
- Se ha añadido una entrada para 'PTFE' en el objeto `materialProperties` de `FichaTecnica.jsx` para que la ficha técnica se muestre cuando se seleccione 'Teflón'.
- Se ha revertido la modificación del botón "Agregar al carrito" en `OringsSearchResults.jsx` a su estado original, incluyendo el texto y el estilo en línea, y se ha revertido la importación de `FaShoppingCart`.
- Se ha cambiado el texto del botón de "Agregar al carrito" a "Agregar" en `OringsSearchResults.jsx`.
- Se ha ajustado el estilo del botón 'Agregar' en `OringsSearchResults.jsx` para que coincida con el tamaño de referencia de `ProductTable.jsx`, aplicando `padding: "7px 20px"` y `fontSize:"0.9em"`, manteniendo `transition` y `borderRadius`.
- Se ha eliminado la propiedad `borderRadius` del estilo en línea del botón 'Agregar' en `OringsSearchResults.jsx` para permitir que el estilo `border-radius` de la clase `sm-btn` se aplique correctamente.